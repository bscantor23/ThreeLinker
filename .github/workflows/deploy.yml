name: Deploy ThreeLinker

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deployment'
        required: false
        default: false
        type: boolean

jobs:
  deploy:
    name: Deploy to Server
    runs-on: [three-linker]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Limpiar logs de nginx antes de build
        run: |
          sudo rm -rf nginx/logs/* || true

      - name: Setup Docker permissions
        run: |
          # Add user to docker group
          sudo usermod -aG docker $USER
          # Change socket permissions
          sudo chmod 666 /var/run/docker.sock
          # Refresh group membership
          newgrp docker
          # Verify Docker access
          docker --version
          
      - name: Create environment file
        run: |
          echo "VITE_SERVER_URL=${{ vars.DOMAIN }}" > .env
          echo "PORT=3001" >> .env
          
      - name: Complete system cleanup before deploy
        run: |
          # Stop all containers and remove volumes
          docker-compose down -v --remove-orphans || true
          
          # Aggressive Docker cleanup
          docker system prune -f --volumes --all || true
          
          # Clean npm cache and node_modules (pero NO borrar package-lock.json)
          npm cache clean --force || true
          rm -rf node_modules || true
          
          # Clean build artifacts and logs
          rm -rf dist build logs nginx/cache || true
          
          # Check disk space
          df -h
          docker system df

      - name: Ensure Docker network exists
        run: |
          docker network inspect threelinker-network >/dev/null 2>&1 || docker network create threelinker-network
          
      - name: Build and start services WITHOUT SSL first
        env:
          # Esta variable se pasa al docker-compose (frontend usa ${VITE_SERVER_URL})
          VITE_SERVER_URL: ${{ vars.DOMAIN }}
        run: |
          docker-compose build --no-cache
          # Start services EXCEPT nginx (we'll start nginx separately without SSL)
          docker-compose up -d redis threelinker-server-1 threelinker-server-2 frontend
          
      - name: Start nginx with existing config (handles SSL conditionally)
        run: |
          # Use the existing nginx.conf which has conditional SSL logic
          docker-compose up -d nginx
          
          # Wait for nginx to be ready
          echo "Waiting for nginx to start..."
          sleep 10
          
          echo "✅ Nginx started with conditional SSL configuration (HTTP disponible)"
          
      - name: Verify domain connectivity
        run: |
          # Verificar conectividad del dominio
          echo "🌐 Verificando conectividad del dominio..."
          DOMAIN="threelinker.genodev.com.co"
          
          # Verificar DNS
          echo "📡 Verificando DNS para $DOMAIN..."
          nslookup $DOMAIN || echo "⚠️  DNS lookup failed"
          
          # Verificar conectividad HTTP externa (puerto 80 contra nginx)
          echo "🔌 Verificando conectividad HTTP..."
          curl -v --connect-timeout 10 http://$DOMAIN/.well-known/acme-challenge/test || echo "⚠️  HTTP connection failed"
          
          # Verificar que nginx responda internamente
          echo "🏠 Verificando nginx interno..."
          docker-compose exec nginx curl -f http://localhost/.well-known/acme-challenge/test || echo "⚠️  Internal nginx check failed"

      - name: Obtener/renovar certificado SSL con Certbot
        env:
          CERTBOT_EMAIL: bscantor3@gmail.com
          DOMAIN: threelinker.genodev.com.co
        run: |
          # Verificar que nginx esté funcionando antes de solicitar certificados
          echo "🔍 Verificando que nginx esté accesible antes del challenge..."
          timeout 30 bash -c 'until curl -f http://localhost/.well-known/acme-challenge/test 2>/dev/null; do sleep 2; done' || echo "⚠️  Nginx no está completamente listo, continuando..."
          
          # Solicita el certificado usando standalone mode (usa directamente el puerto 80 del host)
          echo "🔐 Solicitando certificado SSL (standalone)..."
          docker-compose stop nginx || true
          sleep 5
          
          docker-compose run --rm --service-ports certbot \
            certonly --standalone \
            --email "$CERTBOT_EMAIL" \
            --agree-tos --no-eff-email \
            -d "$DOMAIN"
          
          echo "✅ Certificado solicitado, levantando de nuevo nginx..."
          docker-compose start nginx
          sleep 10

      - name: Recargar nginx tras certbot
        run: |
          docker-compose exec nginx nginx -s reload || true
          echo "🔁 Nginx recargado tras obtener/renovar certificados"

      - name: SSL Setup Troubleshooting
        if: failure()
        run: |
          echo "🔧 Troubleshooting SSL setup..."
          echo "=== Container Status ==="
          docker-compose ps
          echo "=== Nginx Logs ==="
          docker-compose logs nginx --tail=50 || true
          echo "=== Certbot Logs ==="
          docker-compose logs certbot --tail=50 || echo "No certbot logs found"
          echo "=== Network Status ==="
          docker network ls
          docker network inspect threelinker-network || echo "Network not found"
          
          echo "💡 SSL setup failed, but deployment can continue without SSL"
          echo "🌐 Site will be available on HTTP until SSL is manually configured"
          
      - name: Verify deployment
        run: |
          echo "✅ Verificando contenedores en ejecución..."
          docker-compose ps
          
      - name: Post-deploy cleanup
        if: always()
        run: |
          # Clean up after deployment
          docker system prune -f || true
          npm cache clean --force || true
          
          # Show final disk usage
          echo "=== Final Disk Usage ==="
          df -h
          echo "=== Final Docker Usage ==="
          docker system df

      - name: Show service logs
        if: failure()
        run: |
          echo "=== Container Status ==="
          docker-compose ps
          echo "=== Service Logs (últimos 50 logs por servicio) ==="
          docker-compose logs --tail=50 || true
